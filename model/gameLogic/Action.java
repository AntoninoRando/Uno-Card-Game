package model.gameLogic;

/**
 * Possible actions performable by the user and AI during the game.
 */
public enum Action {
    FROM_DECK_DRAW,
    FROM_HAND_PLAY_CARD,
    FROM_HAND_PLAY_TAG,
    SAY_UNO
}
